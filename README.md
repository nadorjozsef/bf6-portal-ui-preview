# Prerequisites

- Node.js (latest LTS recommended)

# Setup

1. Clone the repository:
   ```sh
   git clone https://github.com/nadorjozsef/bf6-portal-ui-preview.git
   ```
2. Navigate into the project directory:
   ```sh
   cd bf6-portal-ui-preview
   ```
3. Install dependencies:
   ```sh
   npm i
   ```
4. Start the server:

   ```sh
   node server.mjs
   ```

5. To develop, make changes in `src/sandbox.js` and the changes will be reflected automatically in the browser.

# bf6-portal-ui-preview

UI Renderer for Battlefield 6 Portal
